# KINSEI 現行テスト実行結果

実行日: 2026-07-10

対象: 現在のワークツリー（未コミット変更を含む）

テストケース: [`current-test-case-inventory.md`](./current-test-case-inventory.md) 全378件

ローカル修正結果: [`current-test-fix-report-20260711.md`](./current-test-fix-report-20260711.md)

## 1. 結果サマリー

| 判定 | 件数 | 意味 |
| --- | ---: | --- |
| PASS | 278 | ブラウザ、HTTP、DB/RLS、ビルド、lint、または同等の境界確認で期待結果を確認 |
| FAIL | 28 | 期待結果と不一致、または明確な実装不備を再現 |
| BLOCKED | 47 | 実メール受信、未用意ブラウザ、実機、DB直結権限など、現在の環境では完遂不可 |
| HOLD | 2 | 仕様差分 GAP-06 の決定待ち |
| STATIC | 23 | 実行ケースではなくコード・設定の静的確認として判定 |
| **合計** | **378** | 全ケースに判定を付与 |

判定ルール上、BLOCKED、HOLD、STATICをPASSには含めていない。

## 2. リリース前に優先して直すべき不合格

### P0: 認可・主要導線

| 対象ID | 結果と再現内容 |
| --- | --- |
| SC-005〜SC-008 | `students` の有効企業向け／本人向け参照が `permission denied for table students` で失敗する。列権限とRLSポリシー内の `students.auth_user_id` 参照が整合していない。 |
| SC-018、SC-019 | 学生本人の連絡履歴SELECTも同じ権限エラーになり、自他分離以前に一覧取得できない。 |
| ST-015、ST-016、CT-037、CT-038 | `/student/contacts` が権限エラーを表示し、空状態・本人履歴・並び順・状態文言を確認できない。 |
| MB-001、MB-002、MB-009、MB-010 | active/trial企業の `/members/students` が権限エラーになり、公開学生一覧と会員限定プロフィールを閲覧できない。past_due/suspended/cancelledの遮断は正常。 |
| PW-010 | 無効な確認リンクでも既存ログインセッションがあるとパスワード更新フォームが有効なまま表示される。 |
| PW-019 | デスクトップ幅では会員ヘッダーのメニューが非表示のため、ログアウト操作に到達できない。 |

### P1/P2: UI・運用

| 対象ID | 結果と再現内容 |
| --- | --- |
| NV-002〜NV-005 | デスクトップ幅でヘッダーの企業ログイン、学生ログイン、マイページ、ログアウトが表示されない。`details` は閉じたままで、`summary` がデスクトップCSSにより非表示。 |
| AS-022、AS-023 | 接続先Supabaseに `student-media` bucketが存在せず、画像・動画アップロードが `Bucket not found` になる。 |
| AH-012 | `updateRequestStatus` が更新対象行数を確認しないため、存在しないrequestIdでも成功扱いになり得る。 |
| QA-004 | 390px幅のログイン済み企業詳細で横幅398pxとなり、求人詳細・メールプレビューに横スクロールが発生する。 |
| QA-007 | 390px幅の管理画面で企業編集フォームが489px、学生編集フォームが399pxとなり横にはみ出す。 |
| QA-014 | 管理者ログイン画面に `h1` が2つある。 |
| QA-018 | TOP、企業一覧、協賛一覧などで44px未満の主要タップ領域を複数確認した。 |
| QA-026 | 存在しないロゴURLでは画像が壊れたままで、フォールバック表示がない。 |

## 3. 追加で確認した仕様差分

- GAP-05: 連絡導線の履歴には同意日時が保存されるが、学生レコードの `profile_share_consent_at` は更新されない。
- GAP-06: `published_at` が未来、または `closed_at` が過去でも、状態が `published` なら求人が公開画面に表示される。CO-027、CO-028は仕様決定待ち（HOLD）。
- GAP-08: `sponsors_list.html` に `Enterprise Logo`、`Company Logo`、`⚪︎⚪︎⚪︎` などの仮表示が残っている。

## 4. BLOCKED 47件

以下はアプリ不合格とはせず、必要な外部環境・操作がないため未完遂とした。

- メール受信、実際のパスワード再設定リンク、期限切れリンク: AU-009、AU-010、PW-004〜PW-009、PW-014、PW-017、PW-018
- 実メールアプリ・mailto互換性: CT-031、CT-032
- 会員DBの残りの画面詳細: MB-013〜MB-027（基盤となるRLSエラーにより後続確認不能）
- 管理画面の実ファイル・外部連携依存: AD-014、AS-014、AS-018、AS-024、AS-025、AC-007
- DB直結でのみ確認可能なdefault privileges: SC-034、SC-035
- Safari、Firefox、Android実機、支援技術、性能・回線条件等: QA-006、QA-009〜QA-011、QA-013、QA-016、QA-020、QA-022〜QA-025

## 5. STATIC 23件

コード・設定の静的確認として扱ったケース:

`RT-007`、`NV-006〜NV-008`、`AU-006〜AU-008`、`PS-008〜PS-009`、`CT-008〜CT-010`、`CT-015〜CT-020`、`AS-004`、`AS-006`、`AJ-009`、`SC-038`、`QA-019`

## 6. 主な合格確認

- `npm run lint`: エラー0。`<img>` 使用の警告9件。
- `npm run build`: 成功。別コピーでWebpack本番ビルドと本番サーバー起動も成功。
- 公開ルート、旧HTMLリダイレクト、robots、sitemap、OG画像、認証必須リダイレクトをHTTPとブラウザで確認。
- 学生ログイン、誤パスワード、外部URLを使う `next` の無害化、学生状態別の連絡可否を確認。
- 連絡ボタンの同意前後、求人別メールアドレス優先順位、履歴・プロフィールスナップショット、長文メール本文の1800文字省略を確認。
- 管理者の学生・企業・求人CRUD、面談状態更新、アクセスリンク生成、状態変更履歴、操作ログを確認。
- 非管理者の管理画面遮断、非公開列の拒否、直接履歴INSERT拒否、重複・不正状態制約、親企業非公開時の求人非表示、XSSエスケープ、静的成果物へのサービスキー非混入を確認。
- 企業・求人・学生削除時の関連データ挙動と、求人削除時の履歴保持を確認。
- TOP、学生一覧、企業一覧、ログイン、協賛ページは390px幅でページ全体の横はみ出しなし。

## 7. 実行後の状態

- テスト専用データは `codex-test-kinsei-*` の接頭辞で分離して作成した。
- テスト終了後、学生0件、企業0件、求人0件、連絡履歴0件、Authユーザー0件の残件を確認した。
- テスト中に変更した既存フィクスチャのプロフィール、企業URL・ロゴ、求人情報、公開状態、会員状態は元へ復元した。
- 修正、commit、push、デプロイは行っていない。

## 8. 推奨修正順

1. `students` の列権限とRLSポリシーを整合させ、学生履歴と企業会員DBを復旧する。
2. 無効パスワードリンクの既存セッション時挙動と、デスクトップ会員ヘッダーを修正する。
3. `student-media` bucketとStorage policyをmigrationで再現可能にする。
4. 390pxの企業詳細・管理フォーム、見出し構造、タップ領域、画像フォールバックを修正する。
5. GAP-05、GAP-06、GAP-08の仕様・運用判断を確定する。
